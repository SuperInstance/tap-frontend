{
  "name": "tap-frontend",
  "version": "1.0.0",
  "description": "The Tap — an agentic bar frontend",
  "scripts": {
    "test": "node --experimental-vm-modules node_modules/.bin/jest"
  },
  "devDependencies": {
    "jest": "^29.7.0",
    "jest-environment-jsdom": "^29.7.0"
  }
}
